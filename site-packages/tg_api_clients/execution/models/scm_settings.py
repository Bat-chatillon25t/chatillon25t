# coding: utf-8

"""
    test.guide Execution API

    API for accessing execution functionality of test.guide.

    The version of the OpenAPI document: 2.57.0
    Contact: support@tracetronic.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations

__copyright__ = 'Copyright © by tracetronic GmbH, Dresden'
__license__ = (
    "This file is distributed as an integral part of tracetronic's software products "
    'and may only be used in connection with and pursuant to the terms and conditions '
    'of a valid tracetronic software product license.'
)


import json
import pprint
import re  # noqa: F401
from typing import Any, ClassVar, Dict, List, Optional, Set

from pydantic import (BaseModel, ConfigDict, Field, StrictBool, StrictStr,
                      field_validator)
from typing_extensions import Self


class ScmSettings(BaseModel):
    """
    SCM tool specific settings that will override the settings of the SCM tool in the project settings.  If this object is passed along a playbook or execution task source or along a dependency, all settings of the  SCM tool will be overridden.
    """  # noqa: E501

    checkout_strategy: Optional[StrictStr] = Field(
        default='CLEAN', description='Strategy for checkout of the source code', alias='checkoutStrategy'
    )
    submodule_checkout_strategy: Optional[StrictStr] = Field(
        default='RECURSIVE',
        description='The checkout strategy for submodules.  * NONE will not checkout submodules * RECURSIVE will checkout all submodules recursively  This setting does only effect Git sources and dependencies. ',
        alias='submoduleCheckoutStrategy',
    )
    use_lfs: Optional[StrictBool] = Field(
        default=False,
        description='If `true`, activate Git LFS support for this repository.  This setting does only effect Git sources and dependencies. ',
        alias='useLfs',
    )
    disable_shallow_clone: Optional[StrictBool] = Field(
        default=False,
        description='If shallow clone is enabled, git will only fetch the latest/specified revision of a repository before  checkout. If shallow clone is disabled, the whole repositories history will be fetched and the specified  reference will be checked out afterwards.  Default is shallow clone.   ATTENTION: disabling shallow clone might result in much longer checkout durations.  This setting does only effect Git sources and dependencies. ',
        alias='disableShallowClone',
    )
    __properties: ClassVar[List[str]] = [
        'checkoutStrategy',
        'submoduleCheckoutStrategy',
        'useLfs',
        'disableShallowClone',
    ]

    @field_validator('checkout_strategy')
    def checkout_strategy_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['NOCLEAN', 'CLEAN', 'CLONE']):
            raise ValueError("must be one of enum values ('NOCLEAN', 'CLEAN', 'CLONE')")
        return value

    @field_validator('submodule_checkout_strategy')
    def submodule_checkout_strategy_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['RECURSIVE', 'NONE']):
            raise ValueError("must be one of enum values ('RECURSIVE', 'NONE')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ScmSettings from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ScmSettings from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                'checkoutStrategy': obj.get('checkoutStrategy') if obj.get('checkoutStrategy') is not None else 'CLEAN',
                'submoduleCheckoutStrategy': obj.get('submoduleCheckoutStrategy')
                if obj.get('submoduleCheckoutStrategy') is not None
                else 'RECURSIVE',
                'useLfs': obj.get('useLfs') if obj.get('useLfs') is not None else False,
                'disableShallowClone': obj.get('disableShallowClone')
                if obj.get('disableShallowClone') is not None
                else False,
            }
        )
        return _obj
