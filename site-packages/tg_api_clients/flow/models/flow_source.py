# coding: utf-8

"""
    [DEV-PREVIEW] test.guide workflow automation API

    [DEV-PREVIEW] This API might change without previous warning! API for accessing workflow automation functionality of test.guide.

    The version of the OpenAPI document: 0.7.0
    Contact: support@tracetronic.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations

__copyright__ = 'Copyright © by tracetronic GmbH, Dresden'
__license__ = (
    "This file is distributed as an integral part of tracetronic's software products "
    'and may only be used in connection with and pursuant to the terms and conditions '
    'of a valid tracetronic software product license.'
)


import json
import pprint
import re  # noqa: F401
from importlib import import_module
from typing import (TYPE_CHECKING, Any, ClassVar, Dict, List, Optional, Set,
                    Union)

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing_extensions import Annotated, Self

if TYPE_CHECKING:
    from tg_api_clients.flow.models.flow_source_artifact import \
        FlowSourceArtifact
    from tg_api_clients.flow.models.flow_source_scm import FlowSourceScm


class FlowSource(BaseModel):
    """
    FlowSource
    """  # noqa: E501

    source_type: StrictStr = Field(alias='sourceType')
    relative_flow_path: Optional[Annotated[str, Field(strict=True, max_length=255)]] = Field(
        default=None, alias='relativeFlowPath'
    )
    __properties: ClassVar[List[str]] = ['sourceType', 'relativeFlowPath']

    @field_validator('source_type')
    def source_type_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['ARTIFACT', 'SCM']):
            raise ValueError("must be one of enum values ('ARTIFACT', 'SCM')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    # JSON field name that stores the object type
    __discriminator_property_name: ClassVar[str] = 'sourceType'

    # discriminator mappings
    __discriminator_value_class_map: ClassVar[Dict[str, str]] = {
        'ARTIFACT': 'FlowSourceArtifact',
        'SCM': 'FlowSourceScm',
    }

    @classmethod
    def get_discriminator_value(cls, obj: Dict[str, Any]) -> Optional[str]:
        """Returns the discriminator value (object type) of the data"""
        discriminator_value = obj[cls.__discriminator_property_name]
        if discriminator_value:
            return cls.__discriminator_value_class_map.get(discriminator_value)
        else:
            return None

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Union[FlowSourceArtifact, FlowSourceScm]]:
        """Create an instance of FlowSource from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Dict[str, Any]) -> Optional[Union[FlowSourceArtifact, FlowSourceScm]]:
        """Create an instance of FlowSource from a dict"""
        # look up the object type based on discriminator mapping
        object_type = cls.get_discriminator_value(obj)
        if object_type == 'FlowSourceArtifact':
            return import_module('tg_api_clients.flow.models.flow_source_artifact').FlowSourceArtifact.from_dict(obj)
        if object_type == 'FlowSourceScm':
            return import_module('tg_api_clients.flow.models.flow_source_scm').FlowSourceScm.from_dict(obj)

        raise ValueError(
            'FlowSource failed to lookup discriminator value from '
            + json.dumps(obj)
            + '. Discriminator property name: '
            + cls.__discriminator_property_name
            + ', mapping: '
            + json.dumps(cls.__discriminator_value_class_map)
        )
