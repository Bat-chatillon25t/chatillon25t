# coding: utf-8

"""
    [DEV-PREVIEW] test.guide Q-Gates API

    [DEV-PREVIEW] This API might change without previous warning! API for handling Q-gates in test.guide.

    The version of the OpenAPI document: 1.2.0
    Contact: support@tracetronic.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations

__copyright__ = 'Copyright © by tracetronic GmbH, Dresden'
__license__ = (
    "This file is distributed as an integral part of tracetronic's software products "
    'and may only be used in connection with and pursuant to the terms and conditions '
    'of a valid tracetronic software product license.'
)


import json
import pprint
import re  # noqa: F401
from importlib import import_module
from typing import (TYPE_CHECKING, Any, ClassVar, Dict, List, Optional, Set,
                    Union)

from pydantic import BaseModel, ConfigDict, Field
from typing_extensions import Self

from tg_api_clients.q_gates.models.action_type import ActionType

if TYPE_CHECKING:
    from tg_api_clients.q_gates.models.approve_continuation_action import \
        ApproveContinuationAction
    from tg_api_clients.q_gates.models.disapprove_continuation_action import \
        DisapproveContinuationAction
    from tg_api_clients.q_gates.models.pass_action import PassAction
    from tg_api_clients.q_gates.models.stop_action import StopAction


class Action(BaseModel):
    """
    Abstract representation of an action, that can be applied to a Q-gate to change its state.<br> Concrete types are: * StopAction (actionType: STOP), only for ManualQGates * PassAction (actionType: PASS), only for ManualQGates * DisapproveContinuationAction (actionType: DISAPPROVE_CONTINUATION), only for ApprovalQGates * ApproveContinuationAction (actionType: APPROVE_CONTINUATION), only for ApprovalQGates
    """  # noqa: E501

    action_type: ActionType = Field(alias='actionType')
    __properties: ClassVar[List[str]] = ['actionType']

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    # JSON field name that stores the object type
    __discriminator_property_name: ClassVar[str] = 'actionType'

    # discriminator mappings
    __discriminator_value_class_map: ClassVar[Dict[str, str]] = {
        'APPROVE_CONTINUATION': 'ApproveContinuationAction',
        'DISAPPROVE_CONTINUATION': 'DisapproveContinuationAction',
        'PASS': 'PassAction',
        'STOP': 'StopAction',
    }

    @classmethod
    def get_discriminator_value(cls, obj: Dict[str, Any]) -> Optional[str]:
        """Returns the discriminator value (object type) of the data"""
        discriminator_value = obj[cls.__discriminator_property_name]
        if discriminator_value:
            return cls.__discriminator_value_class_map.get(discriminator_value)
        else:
            return None

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(
        cls, json_str: str
    ) -> Optional[Union[ApproveContinuationAction, DisapproveContinuationAction, PassAction, StopAction]]:
        """Create an instance of Action from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(
        cls, obj: Dict[str, Any]
    ) -> Optional[Union[ApproveContinuationAction, DisapproveContinuationAction, PassAction, StopAction]]:
        """Create an instance of Action from a dict"""
        # look up the object type based on discriminator mapping
        object_type = cls.get_discriminator_value(obj)
        if object_type == 'ApproveContinuationAction':
            return import_module(
                'tg_api_clients.q_gates.models.approve_continuation_action'
            ).ApproveContinuationAction.from_dict(obj)
        if object_type == 'DisapproveContinuationAction':
            return import_module(
                'tg_api_clients.q_gates.models.disapprove_continuation_action'
            ).DisapproveContinuationAction.from_dict(obj)
        if object_type == 'PassAction':
            return import_module('tg_api_clients.q_gates.models.pass_action').PassAction.from_dict(obj)
        if object_type == 'StopAction':
            return import_module('tg_api_clients.q_gates.models.stop_action').StopAction.from_dict(obj)

        raise ValueError(
            'Action failed to lookup discriminator value from '
            + json.dumps(obj)
            + '. Discriminator property name: '
            + cls.__discriminator_property_name
            + ', mapping: '
            + json.dumps(cls.__discriminator_value_class_map)
        )
