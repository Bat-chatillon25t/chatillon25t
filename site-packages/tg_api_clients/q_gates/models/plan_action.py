# coding: utf-8

"""
    [DEV-PREVIEW] test.guide Q-Gates API

    [DEV-PREVIEW] This API might change without previous warning! API for handling Q-gates in test.guide.

    The version of the OpenAPI document: 1.2.0
    Contact: support@tracetronic.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations

__copyright__ = 'Copyright © by tracetronic GmbH, Dresden'
__license__ = (
    "This file is distributed as an integral part of tracetronic's software products "
    'and may only be used in connection with and pursuant to the terms and conditions '
    'of a valid tracetronic software product license.'
)


import json
import pprint
import re  # noqa: F401
from typing import Any, ClassVar, Dict, List, Optional, Set

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing_extensions import Self


class PlanAction(BaseModel):
    """
    An action, that can be applied to a Q-gate plan.<br>
    """  # noqa: E501

    action: StrictStr = Field(
        description="Changing the Q-gate plan state to 'IN_EXECUTION', modifications on Q-gates and dependencies are no longer possible. The INVALIDATE action sets the plan state to 'INVALIDATED', indicating that the execution of the plan was aborted (this is a final state and can't be undone). The FINALIZE action sets the plan state to 'FINALIZED', indicating that the execution of the plan was completed (this is a final state and can't be undone). "
    )
    __properties: ClassVar[List[str]] = ['action']

    @field_validator('action')
    def action_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['START_EXECUTION', 'INVALIDATE', 'FINALIZE']):
            raise ValueError("must be one of enum values ('START_EXECUTION', 'INVALIDATE', 'FINALIZE')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of PlanAction from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of PlanAction from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({'action': obj.get('action')})
        return _obj
