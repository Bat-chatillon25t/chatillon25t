# coding: utf-8

"""
    test.guide SCM API

    API for accessing SCM functionality of test.guide.

    The version of the OpenAPI document: 3.5.0
    Contact: support@tracetronic.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations

__copyright__ = 'Copyright © by tracetronic GmbH, Dresden'
__license__ = (
    "This file is distributed as an integral part of tracetronic's software products "
    'and may only be used in connection with and pursuant to the terms and conditions '
    'of a valid tracetronic software product license.'
)


import json
import pprint
import re  # noqa: F401
from importlib import import_module
from typing import (TYPE_CHECKING, Any, ClassVar, Dict, List, Optional, Set,
                    Union)

from pydantic import (BaseModel, ConfigDict, Field, StrictInt, StrictStr,
                      field_validator)
from typing_extensions import Annotated, Self

if TYPE_CHECKING:
    from tg_api_clients.scm.models.git_configuration import GitConfiguration
    from tg_api_clients.scm.models.svn_configuration import SvnConfiguration


class ScmConfiguration(BaseModel):
    """
    ScmConfiguration
    """  # noqa: E501

    id: Optional[StrictInt] = Field(default=None, description='SCM configuration identifier')
    label: Annotated[str, Field(min_length=1, strict=True, max_length=128)] = Field(
        description='Label of the SCM configuration'
    )
    type: StrictStr = Field(description="Type of the scm.  For detailed descriptions see section \"Schemas\". ")
    url: Annotated[str, Field(min_length=1, strict=True, max_length=4096)] = Field(description='URL of the repository')
    checkout_strategy: Optional[StrictStr] = Field(
        default='CLEAN', description='Strategy for checkout of the source code', alias='checkoutStrategy'
    )
    __properties: ClassVar[List[str]] = ['id', 'label', 'type', 'url', 'checkoutStrategy']

    @field_validator('type')
    def type_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['GIT', 'SVN']):
            raise ValueError("must be one of enum values ('GIT', 'SVN')")
        return value

    @field_validator('checkout_strategy')
    def checkout_strategy_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['NOCLEAN', 'CLEAN', 'CLONE']):
            raise ValueError("must be one of enum values ('NOCLEAN', 'CLEAN', 'CLONE')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    # JSON field name that stores the object type
    __discriminator_property_name: ClassVar[str] = 'type'

    # discriminator mappings
    __discriminator_value_class_map: ClassVar[Dict[str, str]] = {'GIT': 'GitConfiguration', 'SVN': 'SvnConfiguration'}

    @classmethod
    def get_discriminator_value(cls, obj: Dict[str, Any]) -> Optional[str]:
        """Returns the discriminator value (object type) of the data"""
        discriminator_value = obj[cls.__discriminator_property_name]
        if discriminator_value:
            return cls.__discriminator_value_class_map.get(discriminator_value)
        else:
            return None

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Union[GitConfiguration, SvnConfiguration]]:
        """Create an instance of ScmConfiguration from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * OpenAPI `readOnly` fields are excluded.
        """
        excluded_fields: Set[str] = set(
            [
                'id',
            ]
        )

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Dict[str, Any]) -> Optional[Union[GitConfiguration, SvnConfiguration]]:
        """Create an instance of ScmConfiguration from a dict"""
        # look up the object type based on discriminator mapping
        object_type = cls.get_discriminator_value(obj)
        if object_type == 'GitConfiguration':
            return import_module('tg_api_clients.scm.models.git_configuration').GitConfiguration.from_dict(obj)
        if object_type == 'SvnConfiguration':
            return import_module('tg_api_clients.scm.models.svn_configuration').SvnConfiguration.from_dict(obj)

        raise ValueError(
            'ScmConfiguration failed to lookup discriminator value from '
            + json.dumps(obj)
            + '. Discriminator property name: '
            + cls.__discriminator_property_name
            + ', mapping: '
            + json.dumps(cls.__discriminator_value_class_map)
        )
